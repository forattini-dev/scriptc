import type {
  FlagSchema,
  ParseFlagsResult,
} from "../../typed-workspace-flags-source.js";

export type { FlagSchema } from "../../typed-workspace-flags-source.js";

export declare function parseFlags<Schema extends FlagSchema>(
  argv: readonly string[],
  schema: Schema,
): ParseFlagsResult<Schema>;
