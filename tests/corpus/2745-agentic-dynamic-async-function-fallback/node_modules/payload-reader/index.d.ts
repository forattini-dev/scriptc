export interface ReadOptions {
  readonly deadlineMs?: number;
  readonly stream?: { pause: () => unknown };
}

export type Payload = null | {
  cwd?: string;
  meta: { count?: number };
};

export function readPayload(options?: ReadOptions): Promise<Payload>;
