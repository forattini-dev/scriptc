export function describe(value) {
  if (value === undefined) return "undefined";
  if (value instanceof Uint8Array) return `bytes:${value.join(",")}`;
  return [value instanceof URL, value.href, value.protocol, value.hostname, value.pathname, value.search, value.hash, String(value)].join("|");
}
