// An ES build (the package's "module" field) that still reaches for
// require at top level, as rollup-era dual packages do.
var helper = require('./helper.js');
function render (s) { return helper.shout(s) }
var kind = 'esm';
export { render, kind };
