// @no-engine
// Former dynamic-surface diagnostic: all of these operations now compile.
const up = Math.sqrt(2);
const tau = Math.PI * 2;
const price = (19.99).toPrecision(4);
const swapped = "banana".replace("an", "AN");
const ch = "hello".at(0);
const input: any = "3.14";
const n = Number.parseFloat(input);
void n;
console.log(up, tau, price, swapped, ch, n);
