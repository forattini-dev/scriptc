export interface ReadDeps {
  readonly nowMs?: () => number;
  readonly readCache?: (root: string) => string | null;
}

export interface Facts {
  basename: string;
  count?: number;
}

export function readFacts(root: string, deps?: ReadDeps): Promise<Facts>;
