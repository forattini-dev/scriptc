"use strict";
// A SECOND requirer of the same document: Node answers it from the module
// cache, so both bindings name one value — the compiler bakes ONE global
// per document, which is the same discipline.
const meta = require("../meta.json");

exports.tag = function tag() {
  return meta.label + "/" + meta.release;
};
