export function parse() {
  return { options: { alpha: "one", beta: true } };
}

export function empty() {
  return {};
}

export function readType(options, name) {
  return options[name].type;
}

export function parseErrors() {
  return { errors: [] };
}
