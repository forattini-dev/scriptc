export declare class Counter {
  constructor(start: number);
  value: number;
  bump(step: number): number;
}

export declare class Marker {}

export declare const armed: { label: string; ready?: () => string };
export declare const idle: { label: string; ready?: () => string };
