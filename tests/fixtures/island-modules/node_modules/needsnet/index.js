// node:net needs an event loop inside the island, which the Rust bridge does
// not carry, so its part stays out of the "rust" manifest and requiring it is
// the honest does-not-provide throw.
const net = require("node:net");
module.exports.name = function name() {
  return typeof net.createServer;
};
